export enum PackageManager {
  PNPM = 'pnpm',
  NPM = 'npm',
  // YARN = 'yarn',
  BUN = 'bun',
}
export enum OsType {
  Windows = 'windows',
  Linux = 'linux',
  Mac = 'mac',
  Undefined = 'undefined',
}
