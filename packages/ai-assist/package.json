{
  "name": "ai",
  "version": "0.2.0-aplha.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm build && node ../../scripts/ai-assist-worker.cjs",
    "test": "exit 0",
    "build": "run-p build:cli",
    "build:cli": "run-p verify && zx ./scripts/build-ai-assist.mjs",
    "verify": "tsc -p ./tsconfig.build-cli.json"
  },
  "dependencies": {
    "openai": "^6.17.0"
  }
}
